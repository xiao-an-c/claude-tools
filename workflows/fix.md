---
name: fix
display_name: "Bug 修复"
description: "架构师诊断根因，开发者实施修复。包含文档、验证、验收的完整 bug 修复流程。"
category: code-change
argument_hint: "[--git|--no-git] <问题描述>"
defaults:
  use_git: true
  base_branch: develop
  branch_type: fix
  config_depth: minimal
---

# Workflow: Bug 修复

## 适用场景

- 已知 bug，需要诊断根因并修复
- 线上问题快速响应（非 hotfix 级别）
- 用户反馈的缺陷，需要分析+修复
- 需要比 patch 更严格的流程（文档、验证、验收）

## 不适用

- 一行修改、配置调整（用 patch）
- 紧急线上 hotfix（用 hotfix）
- 大规模重构（用 refactor）

## 预期耗时

5-10 分钟（2 次主要 Agent spawn + 验证）

## 状态管理

创建 `.dev/plan/fix-<slug>/` 目录，写入 PRD.md、PLAN.md、TASK-LOG.md、ACCEPTANCE.md。

## 步骤

### Step 1: 解析输入

Type: builtin
Action: parse_arguments

```
1. 扫描参数中的 --git 和 --no-git 标志
2. 如果同时存在 --git 和 --no-git，报错退出
3. 设置变量：
   - --git      -> use_git = true
   - --no-git   -> use_git = false
   - 未指定     -> use_git = true（fix 模式默认创建分支）
4. 从剩余文本中提取 bug_description
5. 如果没有 bug_description，用 AskUserQuestion 询问用户要修复什么 bug
6. 从 bug 描述中提取关键词，生成短横线连接的 slug（如 "登录白屏" -> "login-blank-screen"）
```

### Step 2: 创建分支

Type: builtin
Action: create_branch
Condition: use_git=true

```bash
# 检测 develop 分支是否存在
git rev-parse --verify develop >/dev/null 2>&1
if [ $? -ne 0 ]; then
  if git rev-parse --verify main >/dev/null 2>&1; then
    BASE_BRANCH=main
  elif git rev-parse --verify master >/dev/null 2>&1; then
    BASE_BRANCH=master
  else
    BASE_BRANCH=$(git branch --show-current)
  fi
else
  BASE_BRANCH=develop
fi

# 检查工作区是否干净，不干净则 stash
STASH_NEEDED=false
if ! git diff --quiet || ! git diff --cached --quiet || [ -n "$(git ls-files --others --exclude-standard)" ]; then
  STASH_NEEDED=true
  git stash push -m "dev-fix-auto-stash" --include-untracked
fi

# 切换到 base 分支并拉取最新
git checkout "$BASE_BRANCH"
git pull origin "$BASE_BRANCH" 2>/dev/null || true

# 检查分支是否已存在
if git show-ref --verify --quiet "refs/heads/fix/$SLUG"; then
  git checkout "fix/$SLUG"
else
  git checkout -b "fix/$SLUG"
fi

# 恢复 stash
if [ "$STASH_NEEDED" = true ]; then
  git stash pop 2>/dev/null || true
fi
```

**记录：** `BASE_BRANCH` 和 `CURRENT_BRANCH="fix/$SLUG"` 用于后续步骤。

### Step 3: 初始化状态

Type: builtin
Action: init_state

```bash
mkdir -p .dev/plan/fix-$SLUG
```

**创建 `.dev/config.yml`：**

```yaml
# Auto-generated by fix workflow. Do not edit manually.

git:
  base_branch: ${base_branch}
  current_branch: ${current_branch}
  slug: ${slug}
  branch_type: fix

workflow:
  mode: fix
  use_git: ${use_git}

project:
  type: null
  language: null
  framework: null
  ui_library: null
  bundler: null
  runtime: null
  dependency_manager: null
  monorepo: false

source_layout:
  root: null
  entry_points: []
  directories: []

build:
  command: null
  type_check_command: null
  lint_command: null
  dev_command: null

test:
  framework: null
  unit_command: null
  single_test_command: null
  config_file: null

verification:
  commands: []

conventions:
  commit_format: null
```

**创建 `.dev/plan/fix-$SLUG/TASK-LOG.md`：**

```markdown
# Task Log: fix/${slug}

| Step | Agent | Status | Summary |
|------|-------|--------|---------|
| 诊断 | dev-architect | pending | - |
| 规划 | dev-planner | pending | - |
| 修复 | dev-developer | pending | - |
| 记录 | dev-recorder | pending | - |
```

### Step 4: 架构师诊断

Type: agent
Agent: dev-architect
Model: opus
Spawn: inline

```
<bug_description>${bug_description}</bug_description>
<project_root>${project_root}</project_root>
<config_path>.dev/config.yml</config_path>
<prd_path>.dev/plan/fix-${slug}/PRD.md</prd_path>

你是架构师，当前任务是 **bug 诊断**（不是架构设计）。

请执行以下分析：

1. **复现路径**: 根据描述，推断 bug 的触发条件和复现步骤
2. **根因分析**: 阅读相关源代码，定位 bug 的根本原因
3. **影响范围**: 列出所有受影响的文件和模块
4. **修复方案**: 提出具体的修复方法，每个文件需要改什么、为什么
5. **风险评估**: 修复可能引入的副作用，需要回归测试的范围

将诊断结果写入 <prd_path>，格式为 Bug Report（不是产品需求文档）：

```markdown
# Bug Report: <bug 简述>

## 症状
<用户描述的 bug 现象>

## 根因
<根本原因分析>

## 受影响文件
- <file1>: <为什么受影响>
- <file2>: <为什么受影响>

## 修复方案
- <file1>: <具体改动>
- <file2>: <具体改动>

## 风险点
- <风险1>
- <风险2>
```

同时更新 <config_path> 中的 project、source_layout、build、test、verification、conventions 段（根据项目实际情况自动发现填充）。

完成后返回简要摘要：根因（1 句话）+ 受影响文件数 + 修复方案概述。
```

**Agent 返回后：**

1. 确认 `.dev/plan/fix-$SLUG/PRD.md` 已生成
2. 确认 `.dev/config.yml` 的项目信息段已填充
3. 提取关键信息用于后续步骤：根因摘要、受影响文件列表、修复方案概述
4. 更新 TASK-LOG.md 诊断行状态为 done

### Step 5: 任务规划

Type: agent
Agent: dev-planner
Model: opus
Spawn: inline

```
<project_root>${project_root}</project_root>
<prd_path>.dev/plan/fix-${slug}/PRD.md</prd_path>
<plan_path>.dev/plan/fix-${slug}/PLAN.md</plan_path>

你是规划师。基于 PRD.md（Bug Report），生成实现计划。

1. 读取 PRD.md，理解 bug 根因和修复方案
2. 将修复方案分解为具体的实现任务
3. 每个 task 包含：标题、描述、涉及文件

输出 PLAN.md：

# 实现计划: fix/${slug}

## 任务列表

### T-01: <任务标题>
- **描述**: <具体做什么>
- **文件**: <涉及文件列表>
- **复杂度**: low/medium
- **依赖**: 无

## 执行顺序
T-01 → ...

注意：
- 这是 bug 修复，任务应该最小化，只修复问题不做额外重构
- 涉及交互变更的任务需标注 `[UI]`
```

**Agent 返回后：**

1. 确认 `.dev/plan/fix-$SLUG/PLAN.md` 已生成
2. 提取任务数量和文件范围
3. 更新 TASK-LOG.md 规划行状态为 done

### Step 6: 开发者修复

Type: agent
Agent: dev-developer
Model: sonnet
Spawn: inline

```
<project_root>${project_root}</project_root>
<config_path>.dev/config.yml</config_path>
<prd_path>.dev/plan/fix-${slug}/PRD.md</prd_path>
<plan_path>.dev/plan/fix-${slug}/PLAN.md</plan_path>

你是开发者。当前任务是 **bug 修复**。

根据 PLAN.md 中的任务列表，逐个实现修复。参考 PRD.md 中的诊断报告。

要求：
- 严格按照 PLAN.md 中的任务逐个实现
- 修复应该是最小化的：只改必要的代码，不做额外重构
- 如果修复过程中发现新问题或方案需要调整，在返回摘要中说明
- 实现完成后提交代码（commit message 格式: fix(<scope>): <简洁描述>）

<user_adjustments>
${user_adjustments}
</user_adjustments>
```

**Agent 返回后：**

1. 更新 TASK-LOG.md 修复行状态为 done，填入摘要
2. 记录 developer 返回的关键信息：实际修改了哪些文件、是否有方案调整

### Step 7: 验证

Type: builtin
Action: verify
Probing: true
Retry: 2

读取 `.dev/config.yml` 的 `verification.commands`。如果为空，依次检查 `build.command`、`lint_command`、`test.unit_command`。

```bash
# 从 config.yml 读取验证命令并执行
# 如果 verification.commands 非空，执行每个命令
# 如果为空，尝试执行 build + lint + test（如果配置了的话）
```

**验证结果处理：**

| 结果 | 动作 |
|------|------|
| 全部通过 | 进入 Step 7 |
| 有失败 | 将失败输出传回 developer agent，要求修复验证问题（最多重试 2 轮） |
| 重试 2 轮仍失败 | 记录失败状态，进入 Step 7，在 ACCEPTANCE.md 中标注 |

**如果没有配置任何验证命令，跳过此步骤。**

### Step 8: 记录

Type: agent
Agent: dev-recorder
Model: sonnet
Spawn: inline

**收集 recorder 需要的数据：**

```bash
# 获取变更文件列表
CHANGED_FILES=$(git diff --name-only $BASE_BRANCH..HEAD 2>/dev/null || git diff --name-only HEAD~1..HEAD)

# 获取 commit hash
COMMIT_HASH=$(git log --oneline -1 --format="%h")

# 获取完整 commit 信息
COMMIT_LOG=$(git log --oneline $BASE_BRANCH..HEAD 2>/dev/null || git log --oneline -5)
```

```
<project_root>${project_root}</project_root>
<config_path>.dev/config.yml</config_path>
<knowledge_dir>docs/knowledge/</knowledge_dir>
<task_log_path>.dev/plan/fix-${slug}/TASK-LOG.md</task_log_path>

你是经验记录员。记录本次 bug 修复的完整信息。

**修复信息：**
- Bug 描述: ${bug_description}
- 根因: ${root_cause_summary}
- 修复方案: ${fix_plan_summary}
- 变更文件: ${changed_files}
- Commit: ${commit_hash}
- Commit 日志: ${commit_log}
- 验证结果: ${verification_result}

更新 TASK-LOG.md，填写所有步骤的状态和摘要。

完成后返回简要记录摘要。
```

**Agent 返回后：** 确认 TASK-LOG.md 已更新。

### Step 9: 验收

Type: builtin
Action: generate_acceptance

生成验收说明书 `.dev/plan/fix-$SLUG/ACCEPTANCE.md`。

**从 PRD.md、TASK-LOG.md 和 git log 提取信息，生成验收文档：**

```markdown
# 验收说明书: fix/${slug}

## Bug 症状

${bug_symptoms}

## 根因分析

${root_cause}

## 修复内容

${fix_content}

## 变更文件

${changed_files_list}

## 验证结果

| 验证项 | 结果 |
|--------|------|
| build | ${build_result} |
| lint | ${lint_result} |
| test | ${test_result} |

## 提交记录

${commit_log}

## 风险提示

${risk_notes}
```

### Step 10: 最终摘要

Type: builtin
Action: display_summary

```
================================================================
 FIX WORKFLOW COMPLETE
================================================================
 Bug: ${bug_description}
 分支: ${current_branch}
 根因: ${root_cause_summary}
 修复: ${fix_summary}
 变更: ${changed_files_count} 个文件
 验证: ${verification_result}
 提交: ${commit_hash}
 验收说明书: .dev/plan/fix-${slug}/ACCEPTANCE.md

 下一步:
   查看验收说明书 -- 确认修复是否符合预期
   /dev:status     -- 查看工作流状态
   /git:finish     -- 合并到 ${base_branch} （如果 use_git=true）
================================================================
```

**如果 use_git=true：** 额外提示 `使用 /git:finish 完成分支合并。`

**如果 use_git=false：** 提示 `代码已提交到当前分支。请手动检查变更。`
